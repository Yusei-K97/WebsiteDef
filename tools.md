// map関数

{.map((props) => {
                return src={props.}
                })}


// props 受け渡し

export const  = (props) => {
    return(
        <Center>{props.}</Center>
    );
};

// ReactHook

    const [ isLoading, setIsloading ] = useState(true);

    useEffect (()=>{
        setTimeout(() => setIsloading(false), 0);
    },[])